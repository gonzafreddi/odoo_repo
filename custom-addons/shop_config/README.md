# Suplex Shop Config

This addon adds the online shop's free-shipping settings to Sales configuration. It stores `shop_config.free_shipping_threshold`, `shop_config.default_shipping_cost`, and `shop_config.free_shipping_enabled` in `ir.config_parameter`; the online shop backend reads them through XML-RPC.
